<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Tarefas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class="container mt-5">
        <h1>Listas de Tarefas</h1>
        <ul  class="list-group">
            <% listas.forEach(lista => { %>
                <li data-id="<%= lista.id %>" class="list-group-item d-flex align-items-center justify-content-between">
                    <article>
                        <a href="/listas/tarefas/<%= lista.id %>" class="text-body-emphasis fs-5 text-decoration-none">
                            <%= lista.title %>
                        </a>
                    <div>
                        <form action="/listas/<%= lista.id %>" method="post">
                            <button type="submit" data-id="<%= lista.id %>" class="btn btn-sm btn-danger">Excluir</button>
                        </form>
                    </div>
                    </article>
                </li>
            <% }); %>
        </ul>
        <a href="/listas/criarLista" class="btn btn-primary mt-3">Nova Lista de Tarefas</a>
    </div>


    

</body>
</html>